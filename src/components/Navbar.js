import Routes from '../Routes';

const Navbar = () => (
  <>
    <nav className="navbar navbar-expand-sm bg-dark navbar-dark">
      <a className="navbar-brand font-weight-bold" href="/">Unsplash</a>
    </nav>

    <Routes />
  </>
);

export default Navbar;
